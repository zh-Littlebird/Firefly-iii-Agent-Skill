任务类型：图片记账（进入 `skills/record.md`）

这类请求必须先识别图片结果，再给你确认，不直接入账。

不过这条 eval 当前没有附上实际图片文件，所以这轮只能验证流程约束，不能给出真实识别结果。

有图片时我会这样处理：
- 先提取商户、金额、日期、支付方式等字段
- 先把识别结果展示给你确认
- 如果时间看不清，会明确标注“缺少精确时间”，而不是默认补成 `00:00:00`
- 只有你确认后才继续提交
